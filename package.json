{
  "private": true,
  "scripts": {
    "check": "npm run check:syntax && npm test",
    "check:syntax": "node --check app.js && node --check theme.js && node --check api/live-snapshot.js && node --check api/fred-snapshot.js",
    "test": "node --test"
  }
}
